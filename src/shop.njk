---
title: Allotment Shop | Allotment Book
description: Find seeds, tools, and gifts for the home gardener or allotment holder
layout: /layouts/pagetemplate.njk
---

        <div id="sidebar">
            a new side
        </div>
    
        <div id="main-column">
        
                <main id="main-content">
                    
                    <div id="content-heading">
                        A new header image here
                    </div>

                    <div id="content-body">
                        Some words go here.

                        {% for veg in varliam.veg %} {% if veg.vegetable == "broccoli" %}
                        <li>{{ veg.name }}</li>
                        {% endif %}{% endfor %}

                        <h3>Test Data</h3>

                        {% for veg in vegdata.veg %} {% if veg.vegetable == "broccoli" %}
                        <li> {{ veg.type }} </li>
                        {% endif %}{% endfor %}

                        <h3>Test More Data</h3>

                        {% for veg in vegdata.veg %} {% if 'featured' in veg.type %}
                        <li> {{ veg.name }}, {{ veg.description }} </li>
                        {% endif %} {% endfor %}

                        {% macro add(x, y) %}
                        {{ caller() }}: {{ x + y }}
                        {% endmacro%}

                        {% call add(1, 2) -%}
                        The result is
                        {%- endcall %}

                    </div> 
                </main>

            </div>